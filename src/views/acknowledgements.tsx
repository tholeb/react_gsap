import React from 'react';
import useTitle from '../components/hooks/useTitle';

const Index = () => {
	useTitle('Acknowledgements');
	return <h1 className='font-bold text-red-500'>ok mezc</h1>;
};

export default Index;
